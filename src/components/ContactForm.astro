---
---
<!--
<section id="formulario-contacto" class="py-20 bg-amber-400">
  <div class="container mx-auto px-4 max-w-4xl text-center">
    <h2 class="text-3xl md:text-5xl font-black mb-4 uppercase">CONOCE CADA DETALLE DE TU VIAJE DÍA POR DÍA</h2>
    <p class="text-xl md:text-2xl font-medium mb-12 text-gray-900">
      Completa el formulario para descargar el itinerario completo en PDF y asegurar la preventa de tu lugar.
    </p>

    <form id="lead-form" class="bg-white p-8 md:p-12 rounded-[2.5rem] shadow-2xl text-left">
      <div class="grid md:grid-cols-1 gap-6">
        <div>
          <label for="name" class="block text-sm font-bold text-gray-700 uppercase tracking-wider mb-2">Nombre Completo</label>
          <input
            type="text"
            id="name"
            name="name"
            required
            placeholder="Ej. Juan Pérez"
            class="w-full px-6 py-4 rounded-xl border-2 border-gray-100 focus:border-amber-400 focus:ring-0 outline-none transition-all text-lg"
          />
        </div>
        <div>
          <label for="phone" class="block text-sm font-bold text-gray-700 uppercase tracking-wider mb-2">Teléfono / WhatsApp</label>
          <input
            type="tel"
            id="phone"
            name="phone"
            required
            placeholder="Ej. +52 55 1234 5678"
            class="w-full px-6 py-4 rounded-xl border-2 border-gray-100 focus:border-amber-400 focus:ring-0 outline-none transition-all text-lg"
          />
        </div>
        <div>
          <label for="email" class="block text-sm font-bold text-gray-700 uppercase tracking-wider mb-2">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="tu@email.com"
            class="w-full px-6 py-4 rounded-xl border-2 border-gray-100 focus:border-amber-400 focus:ring-0 outline-none transition-all text-lg"
          />
        </div>
      </div>

      <button
        type="submit"
        id="submit-btn"
        class="w-full mt-10 bg-black text-white hover:bg-gray-800 font-bold py-5 px-12 rounded-xl text-xl md:text-2xl transition-all duration-300 transform hover:scale-[1.02] shadow-xl flex items-center justify-center gap-3 disabled:opacity-50"
      >
        <span>Descargar itinerario PDF</span>
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
      </button>
      <p id="form-msg" class="mt-4 text-center font-bold hidden"></p>
    </form>
  </div>
</section>

<script>
  const form = document.getElementById('lead-form') as HTMLFormElement;
  const msg = document.getElementById('form-msg');
  const btn = document.getElementById('submit-btn') as HTMLButtonElement;

  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      btn.disabled = true;
      const originalText = btn.innerHTML;
      btn.innerHTML = 'Enviando...';

      const formData = new FormData(form);
      const data = Object.fromEntries(formData);

      try {
        const response = await fetch('/api/contact', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });

        const result = await response.json();

        if (response.ok) {
          msg!.textContent = result.message;
          msg!.className = 'mt-4 text-center font-bold text-green-600 block';
          form.reset();

          // Trigger PDF Download
          const link = document.createElement('a');
          link.href = '/pdf/europa-con-disney.pdf';
          link.download = 'Itinerario_Europa_Disney_2026.pdf';
          document.body.appendChild(link);
          link.click();
          document.body.removeChild(link);

        } else {
          msg!.textContent = result.message || 'Error al enviar.';
          msg!.className = 'mt-4 text-center font-bold text-red-600 block';
        }
      } catch (error) {
        msg!.textContent = 'Error de conexión.';
        msg!.className = 'mt-4 text-center font-bold text-red-600 block';
      } finally {
        btn.disabled = false;
        btn.innerHTML = originalText;
      }
    });
  }
</script>
-->
